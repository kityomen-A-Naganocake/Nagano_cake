<hi>パブリックの、ホームズの、トップ。トップページだよー</hi>

<%= link_to "ログアウト", destroy_customer_session_path, method: :delete %>
<div>
  <!--開発途中　　削除予定-->
  <!--<h2>ジャンル検索</h2>-->
  <%# genre.each do |genre| %>
  <%#= link_to genre.name, item_path(genre.id) %>
  <%# end %>
</div>
<div class="container">
  <div>
    <!--ながのCAKE紹介文-->
    <p>ようこそ、ながのCAKEへ！</p>
    <p>このサイトは、ケーキ販売のECサイトになります。</p>
    <p>会員でないかたも商品の閲覧はできますが、</p>
    <p>購入には会員登録が必要になります。</p>
  </div>
  <div class="col-md-8 offset-md-1">
    <!--ランダムでアイテム４件表示-->
    <h2>新着商品</h2>
        <table class="table table-hover table-inverse">
          <tbody>
            <!--新着を４件表示-->
            <% @items.first(4). each do |item| %>
            <tr>
              <td>
                <!--アイテム画像表示-->
                <%= link_to item_path(item.id) do %>
                  <!--アイテム画像を表示-->
                  <%= attachment_image_tag item, :image, :fill, 150, 150 %>
                <% end %>
                  <!--商品名と税抜き価格のリンク表示-->
                <p><%= link_to item.name, item_path(item.id) %></p>
                <p><%= link_to item_path(item.id) do %>
                   <%= (item.price_no_tax * 1.1).floor %> 円</p>
                   <% end %>
              </td>
            </tr>
            <% end %>
          </tbody>
      </table>
  </div>
  <div>
    <!--アイテム一覧画面に飛ぶ-->
   <%= link_to "全ての商品を見る", items_path %>
  </div>
</div>

