<h1>アドミンのカスタマーのインデックスページ。会員一覧見れて、会員ID、氏名、メールアドレス、ステータス見れるよー</h1>

<div class="container">
  <div class="row">
    <div class="col-md-3">
    </div>
    <div class="col-md-8 offset-md-1">
      <!--会員一覧を表示-->
      <h2>会員一覧</h2>
        <table class="table table-hover table-inverse">
          <thead>
            <th>会員ID</th>
            <th>氏名</th>
            <th></th>
            <th>メールアドレス</th>
            <th>ステータス</th>
          </thead>
          <tbody>
          <!--会員情報を表示-->
          <% @customers.each do |customer| %>
            <tr>
              <!--会員情報のID・氏名（フルネーム）・メールアドレス・退会ステータスを表示-->
              <td><%= customer.id %></td>
              <td><%= link_to customer.full_name, admin_customer_path(customer) %><td>
              <td><%= customer.email %></td>
              <!--退会ステータス（イーナム）追加予定-->
              <td><%#= customer.is_deleted %></td>
            </tr>
          <% end %>
          </tbody>
      </table>
      <div>
        <!--kaminariで１０件表示 -->
        <%= paginate @customers %>
      </div>
    </div>
  </div>
</div>